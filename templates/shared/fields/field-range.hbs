{{!-- Range --}}
<div class="form-group split-group">
    <label>{{ localize "DUST.Range" }}</label>
    <div class="form-fields">

        {{!-- Value --}}
        {{#if range.scalar}}
        {{ formField fields.value value=data.value label="DUST.Value" localize=true hint=false classes="label-top" }}
        {{/if}}

        {{!-- Units --}}
        {{ formField fields.units value=data.units label="DUST.MovementUnits" localize=true hint=false localize=true
                     options=rangeTypes classes="label-top" }}
    </div>

    {{!-- Condition --}}
    {{ formInput fields.special value=data.special input=inputs.createTextInput localize=true classes="full-width"
                 placeholder=(localize "DUST.RANGE.FIELDS.range.special.label") }}
</div>
